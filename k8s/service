apiVersion: v1
kind: Service
metadata:
  name: monitor-service
spec:
  type: NodePort
  selector:
    app: monitor
  ports:
    - port: 3000       # Porta do serviço interno
      targetPort: 3000 # Porta do container
      nodePort: 30080  # Porta do host (para acessar via navegador)
